name: ACC Competitive Residual Scheduler

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. GS-Sub recurring search was economically dominated by Proof Atlas / official-move connector search."
