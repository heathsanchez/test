name: ACC Competitive Telemetry

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Live competition reads now use the unauthenticated public frontend backend and packed audits."
