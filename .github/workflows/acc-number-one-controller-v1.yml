name: ACC Number One Controller V1 (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "ACC Number One Controller V1 is retired."
          echo "Use ACC Points Controller V2 for strict-first live frontier acquisition."
