name: ARC3 developmental run
on:
  push:
    branches: [arc3-developmental-tonight]
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install ARC SDK
        run: python -m pip install --upgrade pip && pip install arc-agi
      - name: Run true-root cost-dominance search
        shell: bash
        run: |
          set -o pipefail
          python experiments/arc3_tonight/developmental_ls20_v8_cost_dominance.py | tee arc3_developmental_v8_cost_dominance.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: arc3-developmental-v8-cost-dominance
          path: |
            arc3_developmental_v8_cost_dominance.log
            arc3_developmental_v8_cost_dominance_result.json
