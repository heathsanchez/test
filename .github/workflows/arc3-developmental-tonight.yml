name: ARC3 developmental run
on:
  push:
    branches: [arc3-developmental-tonight]
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install ARC SDK
        run: python -m pip install --upgrade pip && pip install arc-agi
      - name: Run one-bit resource ledger on Level4
        shell: bash
        run: |
          set -o pipefail
          python experiments/arc3_tonight/developmental_ls20_v22_one_bit_resource_ledger.py | tee arc3_v22_one_bit_resource.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: arc3-v22-one-bit-resource
          path: |
            arc3_v22_one_bit_resource.log
            arc3_developmental_v22_one_bit_resource_ledger_result.json
