name: Collatz adaptive lawful prefix8 frontier

on:
  push:
    paths:
      - ".github/workflows/collatz-adaptive-prefix8.yml"
  workflow_dispatch:

concurrency:
  group: collatz-adaptive-prefix8
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED"
          echo "Run 34924996326 passed the protected adaptive control but no H803 class resolved."
          echo "Superseded by theorem-strengthened adaptive search with transfer-density lemmas."
