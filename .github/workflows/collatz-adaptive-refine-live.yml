name: Collatz adaptive lawful-prefix refinement live

on:
  push:
    paths:
      - ".github/workflows/collatz-adaptive-refine-live.yml"
  workflow_dispatch:

concurrency:
  group: collatz-adaptive-refine-live
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED"
          echo "Exact budgeted UNKNOWN-to-refine semantics passed protected control."
          echo "At H803, magnitude-heavy state remained the bottleneck even at cheap conflict budgets."
          echo "Superseded by collatz-modular-adaptive.yml using exact shrinking parity state."
