name: Collatz adaptive 23 reverse EO

on:
  push:
    branches: [collatz-valuation-quotient-v1]
    paths:
      - ".github/workflows/collatz-adaptive23-reverse-eo.yml"
      - "experiments/collatz_adaptive23_reverse_eo.cpp"
  workflow_dispatch:

concurrency:
  group: collatz-adaptive23-reverse-eo
  cancel-in-progress: true

jobs:
  adaptive:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - { depth: 12, refine: 0, macro: 6, emax: 5 }
          - { depth: 12, refine: 1, macro: 6, emax: 5 }
          - { depth: 16, refine: 0, macro: 6, emax: 5 }
          - { depth: 16, refine: 1, macro: 6, emax: 5 }
    name: adaptive-d${{ matrix.depth }}-r${{ matrix.refine }}
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: |
          set -euo pipefail
          g++ -O3 -march=native -std=c++20             experiments/collatz_adaptive23_reverse_eo.cpp -o /tmp/adaptive23
      - name: Run adaptive exact quotient
        run: |
          set -euo pipefail
          mkdir -p results
          /usr/bin/time -f '%e %M' -o results/time-memory.txt             /tmp/adaptive23 ${{ matrix.depth }} ${{ matrix.refine }} ${{ matrix.macro }} ${{ matrix.emax }}             | tee results/census.txt
          grep -q "EXACT_ALL_ODD_N_GT_1_PARTITION" results/census.txt
          grep -q "FINAL_GATE=ADAPTIVE23_REVERSE_D${{ matrix.depth }}_R${{ matrix.refine }}_M${{ matrix.macro }}_E${{ matrix.emax }}" results/census.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: adaptive-d${{ matrix.depth }}-r${{ matrix.refine }}
          path: results
          retention-days: 30
