name: Collatz transfer-prefix band k15-k8

on:
  push:
    paths:
      - ".github/workflows/collatz-band-k15-k8.yml"
  workflow_dispatch:

concurrency:
  group: collatz-band-k15-k8
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED"
          echo "Superseded by verifier-gated representation swarm; future compute is allocated only after consequence quotienting."
