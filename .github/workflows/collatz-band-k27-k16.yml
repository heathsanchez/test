name: Collatz merged valuation band k27-k16

on:
  push:
    paths:
      - ".github/workflows/collatz-band-k27-k16.yml"
  workflow_dispatch:

concurrency:
  group: collatz-band-k27-k16
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED"
          echo "Superseded by verifier-gated representation swarm; future compute is allocated only after consequence quotienting."
