name: Collatz bi-adic cell V0

on:
  push:
    branches:
      - collatz-biadic-cell-v0
    paths:
      - 'experiments/collatz_biadic_cell_v0.py'
      - '.github/workflows/collatz-biadic-cell-v0.yml'
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Exact adversarial bi-adic cell quotient audit
        run: |
          set -o pipefail
          mkdir -p evidence/collatz-biadic-cell-v0
          python experiments/collatz_biadic_cell_v0.py             --lo 46081 --hi 46591 --K 128             --output evidence/collatz-biadic-cell-v0/result.json             | tee evidence/collatz-biadic-cell-v0/run.log
      - uses: actions/upload-artifact@v4
        with:
          name: collatz-biadic-cell-v0
          path: evidence/collatz-biadic-cell-v0
          if-no-files-found: error
