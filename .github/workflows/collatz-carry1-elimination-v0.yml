name: Collatz carry1 elimination V0
on:
  push:
    branches: [collatz-universal-source-product-v1]
    paths:
      - 'experiments/collatz_terminal_carry1_elimination_v0.py'
      - 'experiments/collatz_crystal_campaign_state_v1.json'
      - '.github/workflows/collatz-carry1-elimination-v0.yml'
  workflow_dispatch:
jobs:
  certify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          mkdir -p evidence/collatz-carry1-elimination-v0
          python experiments/collatz_terminal_carry1_elimination_v0.py | tee evidence/collatz-carry1-elimination-v0/result.json
          cp experiments/collatz_crystal_campaign_state_v1.json evidence/collatz-carry1-elimination-v0/state.json
      - uses: actions/upload-artifact@v4
        with:
          name: collatz-carry1-elimination-v0
          path: evidence/collatz-carry1-elimination-v0
