name: Exact Collatz Coefficient Feasibility (superseded)

on:
  push:
    paths:
      - ".github/workflows/collatz-coefficient-feasibility.yml"
  workflow_dispatch:

concurrency:
  group: collatz-coefficient-feasibility
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "Superseded by compressed exact sharded frontier run 34921504101"
