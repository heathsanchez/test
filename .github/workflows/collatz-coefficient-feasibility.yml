name: Exact Collatz Coefficient Feasibility (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "Retired; use compressed exact sharded frontier workflow."
