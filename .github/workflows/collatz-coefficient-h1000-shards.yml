name: Collatz Coefficient H1000 Shards V2 (retired)

on:
  push:
    paths:
      - ".github/workflows/collatz-coefficient-h1000-shards.yml"
  workflow_dispatch:

concurrency:
  group: collatz-coefficient-h1000-shards
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "Retired after no separator; replaced by pinned Kissat H1000 portfolio and low-bit exact partition."
