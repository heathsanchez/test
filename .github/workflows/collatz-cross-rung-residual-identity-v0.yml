name: Collatz cross rung residual identity V0
on:
  push:
    branches: [collatz-universal-source-product-v1]
    paths:
      - 'experiments/collatz_cross_rung_residual_identity_v0.py'
      - '.github/workflows/collatz-cross-rung-residual-identity-v0.yml'
  workflow_dispatch:
jobs:
  compare:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          mkdir -p evidence/collatz-cross-rung-residual-identity-v0
          python experiments/collatz_cross_rung_residual_identity_v0.py | tee evidence/collatz-cross-rung-residual-identity-v0/result.json
      - uses: actions/upload-artifact@v4
        with:
          name: collatz-cross-rung-residual-identity-v0
          path: evidence/collatz-cross-rung-residual-identity-v0
