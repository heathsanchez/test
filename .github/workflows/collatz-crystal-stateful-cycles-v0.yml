name: Collatz Crystal stateful cycles V0
on:
  push:
    branches: [collatz-universal-source-product-v1]
    paths:
      - 'experiments/collatz_crystal_stateful_cycles_v0.py'
      - '.github/workflows/collatz-crystal-stateful-cycles-v0.yml'
  workflow_dispatch:
jobs:
  cycles:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          mkdir -p evidence/collatz-crystal-stateful-cycles-v0
          python experiments/collatz_crystal_stateful_cycles_v0.py | tee evidence/collatz-crystal-stateful-cycles-v0/result.json
      - uses: actions/upload-artifact@v4
        with:
          name: collatz-crystal-stateful-cycles-v0
          path: evidence/collatz-crystal-stateful-cycles-v0
