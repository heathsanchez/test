name: Collatz H1000 Kissat portfolio

on:
  push:
    paths:
      - ".github/workflows/collatz-h1000-kissat.yml"
  workflow_dispatch:

concurrency:
  group: collatz-h1000-kissat
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED"
          echo "Superseded by exact goal-native lawful-prefix quotient run 34924571852."
          echo "The coarse four-shard coefficient representation produced no separator before retirement."
