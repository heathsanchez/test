name: Collatz H1000 low-bit partition

on:
  push:
    paths:
      - ".github/workflows/collatz-h1000-lowbits.yml"
  workflow_dispatch:

concurrency:
  group: collatz-h1000-lowbits
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED"
          echo "Superseded by exact goal-native lawful-prefix quotient run 34924571852."
          echo "The monolithic low-4 coefficient partition produced no separator before retirement."
