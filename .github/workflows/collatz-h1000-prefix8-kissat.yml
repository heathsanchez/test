name: Collatz H1000 lawful prefix8 partition

on:
  push:
    paths:
      - ".github/workflows/collatz-h1000-prefix8-kissat.yml"
  workflow_dispatch:

concurrency:
  group: collatz-h1000-prefix8-kissat
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED"
          echo "Run 34924571852 proved the lawful-prefix quotient and protected boundary,"
          echo "then was superseded before any H1000 class resolved."
          echo "The active representation is the adaptive H803 witness-guided prefix frontier."
