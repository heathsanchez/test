name: Collatz theorem-strengthened adaptive prefix8

on:
  push:
    paths:
      - ".github/workflows/collatz-hybrid-prefix8.yml"
  workflow_dispatch:

concurrency:
  group: collatz-hybrid-prefix8
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED"
          echo "The low-8 theorem-strengthened frontier preserved exact controls but remained too coarse at H803."
          echo "Superseded by collatz-hybrid-split12.yml, which refines each lawful low-8 parent into lawful low-12 children using SAT assumptions and learned-clause reuse."
