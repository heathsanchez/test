name: Collatz hybrid split12 assumption frontier

on:
  push:
    paths:
      - ".github/workflows/collatz-hybrid-split12.yml"
  workflow_dispatch:

concurrency:
  group: collatz-hybrid-split12
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED"
          echo "Protected split12 control passed exactly: H182 localized the witness to child 3431; H183 closed all 15 children."
          echo "Live H803 full child solves remained blocking, so the representation was superseded by budgeted adaptive refinement."
