name: Collatz modular adaptive frontier

on:
  push:
    paths:
      - ".github/workflows/collatz-modular-adaptive.yml"
  workflow_dispatch:

concurrency:
  group: collatz-modular-adaptive
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED"
          echo "Shrinking-modular representation passed protected 182/183 control."
          echo "Live H803 low16 probes produced no SAT/UNSAT separators across sampled parents."
          echo "Superseded by exact valuation-slice compilation on the tightened live interval."
