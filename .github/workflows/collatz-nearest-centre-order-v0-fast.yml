name: Collatz nearest centre order V0 fast

on:
  push:
    branches:
      - collatz-nearest-centre-order-v0-fast
    paths:
      - '.github/workflows/collatz-nearest-centre-order-v0-fast.yml'
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Exact adversarial centre-order audit
        run: |
          set -o pipefail
          mkdir -p evidence/collatz-nearest-centre-order-v0-fast
          python experiments/collatz_nearest_centre_order_v0.py             --lo 46081 --hi 46591 --K 128             --output evidence/collatz-nearest-centre-order-v0-fast/result.json             | tee evidence/collatz-nearest-centre-order-v0-fast/run.log
      - uses: actions/upload-artifact@v4
        with:
          name: collatz-nearest-centre-order-v0-fast
          path: evidence/collatz-nearest-centre-order-v0-fast
          if-no-files-found: error
