name: Collatz possibility collapse V0
on:
  push:
    branches: [collatz-universal-source-product-v1]
    paths:
      - 'experiments/collatz_possibility_collapse_v0.py'
      - 'experiments/collatz_crystal_campaign_state_v1.json'
      - '.github/workflows/collatz-possibility-collapse-v0.yml'
  workflow_dispatch:
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          mkdir -p evidence/collatz-possibility-collapse-v0
          python experiments/collatz_possibility_collapse_v0.py | tee evidence/collatz-possibility-collapse-v0/result.json
          cp experiments/collatz_crystal_campaign_state_v1.json evidence/collatz-possibility-collapse-v0/state.json
      - uses: actions/upload-artifact@v4
        with:
          name: collatz-possibility-collapse-v0
          path: evidence/collatz-possibility-collapse-v0
