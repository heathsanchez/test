name: Collatz reverse cost gap V0
on:
  push:
    branches: [collatz-universal-source-product-v1]
    paths:
      - 'experiments/collatz_reverse_cost_gap_v0.py'
      - '.github/workflows/collatz-reverse-cost-gap-v0.yml'
  workflow_dispatch:
jobs:
  gap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          mkdir -p evidence/collatz-reverse-cost-gap-v0
          python experiments/collatz_reverse_cost_gap_v0.py | tee evidence/collatz-reverse-cost-gap-v0/result.json
      - uses: actions/upload-artifact@v4
        with:
          name: collatz-reverse-cost-gap-v0
          path: evidence/collatz-reverse-cost-gap-v0
