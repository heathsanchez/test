name: Collatz exact v2=26 survivor frontier

on:
  push:
    paths:
      - ".github/workflows/collatz-v26-survivor.yml"
  workflow_dispatch:

concurrency:
  group: collatz-v26-survivor
  cancel-in-progress: true

jobs:
  compiled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "COMPILED_SLICE"
          echo "v2(n+1)=26 is retained in experiments/collatz_residual_ledger.json."
          echo "Do not rescan unless the verifier or retained theorem set changes materially."
