name: Collatz exact v2=28 survivor frontier

on:
  push:
    paths:
      - ".github/workflows/collatz-v28-survivor.yml"
  workflow_dispatch:

concurrency:
  group: collatz-v28-survivor
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED"
          echo "Superseded by verifier-gated representation swarm; future compute is allocated only after consequence quotienting."
