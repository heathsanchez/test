name: Collatz exact v2=34 survivor frontier

on:
  push:
    paths:
      - ".github/workflows/collatz-v34-survivor.yml"
  workflow_dispatch:

concurrency:
  group: collatz-v34-survivor
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED"
          echo "Queued v2=34 scan superseded after v2=35 produced a new exact first-descent record of 853."
          echo "Current priority is to deepen the productive v2=35 slice."
