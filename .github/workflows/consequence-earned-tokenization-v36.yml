name: Consequence Earned Tokenization V36

on:
  push:
    branches:
      - consequence-earned-tokenization-v36
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify frozen V36 core
        shell: bash
        run: |
          set -euo pipefail
          FREEZE=00045bf2fc8441742d3a86946a327e490c7a89c9
          git merge-base --is-ancestor "$FREEZE" HEAD
          changed="$(git diff --name-only "$FREEZE"..HEAD --             experiments/consequence_earned_tokenization_v36/PROTOCOL.md             experiments/consequence_earned_tokenization_v36/basis.py             experiments/consequence_earned_tokenization_v36/kernel.py             experiments/consequence_earned_tokenization_v36/FREEZE.json)"
          if [ -n "$changed" ]; then
            echo "Frozen V36 core changed after scientific freeze:"
            echo "$changed"
            exit 1
          fi

      - name: Syntax check
        run: |
          python -m py_compile             experiments/consequence_earned_tokenization_v36/basis.py             experiments/consequence_earned_tokenization_v36/kernel.py             experiments/consequence_earned_tokenization_v36/challenge_pack.py             experiments/consequence_earned_tokenization_v36/run.py

      - name: Run frozen V36 experiment
        working-directory: experiments/consequence_earned_tokenization_v36
        shell: bash
        run: |
          set -o pipefail
          python run.py | tee run.log

      - name: Evidence summary
        if: always()
        working-directory: experiments/consequence_earned_tokenization_v36
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          p=Path("evidence.json")
          if not p.exists():
              print("No evidence.json produced")
              raise SystemExit(1)
          e=json.loads(p.read_text())
          print("verdict:",e.get("verdict"))
          print("full_pass:",e.get("full_pass"))
          for k,v in sorted(e.get("gates",{}).items()):
              print(k,v)
          PY

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: consequence-earned-tokenization-v36-evidence
          path: |
            experiments/consequence_earned_tokenization_v36/evidence.json
            experiments/consequence_earned_tokenization_v36/run.log
            experiments/consequence_earned_tokenization_v36/FREEZE.json
            experiments/consequence_earned_tokenization_v36/PROTOCOL.md
