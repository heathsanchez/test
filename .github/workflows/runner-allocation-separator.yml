name: Runner Allocation Separator

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Acquire hosted runner
        run: |
          echo "runner_acquired=1"
          uname -a
