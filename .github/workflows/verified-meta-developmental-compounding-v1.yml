name: Verified Meta Developmental Compounding V1
on:
  workflow_dispatch:
  push:
    branches: [verified-meta-developmental-compounding-v1]
    paths:
      - 'experiments/verified_meta_developmental_compounding_v1/**'
      - 'experiments/typed_residual_kernel_v1/TypedResidualKernel.lean'
      - '.github/workflows/verified-meta-developmental-compounding-v1.yml'
jobs:
  compounding:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - name: Run level-complete meta-developmental experiment
        run: python experiments/verified_meta_developmental_compounding_v1/run.py
      - name: Install Lean
        run: |
          curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain leanprover/lean4:v4.30.0-rc2
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Verify retained developmental state
        run: |
          set -o pipefail
          lean -o /tmp/TypedResidualKernel.olean experiments/typed_residual_kernel_v1/TypedResidualKernel.lean 2>&1 | tee /tmp/kernel.log
          LEAN_PATH=/tmp lean experiments/verified_meta_developmental_compounding_v1/MetaCompounding.lean 2>&1 | tee /tmp/meta.log
          ! grep -q sorry /tmp/kernel.log
          ! grep -q sorry /tmp/meta.log
      - name: Freeze evidence
        run: |
          cd experiments/verified_meta_developmental_compounding_v1
          sha256sum PROTOCOL.md MetaCompounding.lean run.py results/evidence.json results/snapshot.json > results/SHA256SUMS
          git rev-parse HEAD > results/RUN_COMMIT
          cp /tmp/kernel.log /tmp/meta.log results/
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: verified-meta-developmental-compounding-v1
          path: experiments/verified_meta_developmental_compounding_v1/results
          retention-days: 90
