# V37 Natural Continuation from V36 — Frozen Precommit

## Frozen residual

V36 established that after target-oriented obligation typing the repaired raw transformer carrier has one minimum resolving concrete probe:

`MODEL_EXISTS(4,FORWARD)`.

The next question is not which probe to choose. It is what the generic developmental runtime does after executing the uniquely justified probe on the natural successor state.

## Hypothesis

A discovered, obligation-correct natural probe can be executed through the generic runtime, update the observational quotient only through its verified result, recompute the common lawful continuation, execute that continuation if one exists, and produce a fresh successor route without an experiment-specific handoff.

## Frozen protocol

1. Reconstruct the V36 answer-blind natural SAIR successor state and repaired raw transformer carrier.
2. Recompute all minimum resolving raw transformers under target-oriented obligations.
3. Require exactly one minimum concrete probe and require it to be `MODEL_EXISTS(4,FORWARD)` as an observed V36 fact, not as a route instruction.
4. Before reading the order-4 outcome, choose the first world in the successor cell by deterministic world index.
5. Populate the exact order-4 forward verifier outcome for every world in the current successor cell, independently rechecking every SAT witness.
6. Add only the recomputed unique concrete probe to the current probe language through the generic domain hook.
7. Require the generic router to license PROBE; execute it using `DevelopmentalRuntime.execute_probe`.
8. Require the quotient/hypothesis update to be caused only by that verified probe result.
9. Recompute routing. If ACT is licensed, execute the generic common continuation. Do not prescribe which continuation or whether it terminates.
10. Route the resulting successor again. The evidentiary result records the route; no desired next route is preregistered beyond belonging to the runtime's declared route set.

## Gates

- official natural SAIR corpus used answer-blind;
- V36 unique minimum concrete probe reconstructed;
- actual world chosen before order-4 outcome inspection;
- exact order-4 verifier has zero bad witnesses and zero unknowns;
- generic router licenses the unique probe;
- verified probe execution refines the current cell consistently with the observed outcome;
- post-probe route is recomputed, not hard-coded;
- if ACT is licensed, selected continuation is lawful and executed through the generic runtime;
- successor is first-class and routed again by the same router;
- no protected answer enters routing, probe selection, state update, or continuation selection;
- `V37_NATURAL_CONTINUATION_GATE=true` iff all applicable gates hold.

## Claim boundary

A pass establishes a bounded natural continuation of the V36 developmental trace through a discovered probe and generic runtime. It does not establish long-horizon autonomy, unrestricted grammar invention, or broad SAIR solving performance.