# Procedure composition v2 — frozen diagnostic continuation

Parent execution: commit 1bacd0fcd47554be74a0218c7a621ba0d029ab53, run 34304988491. The v1 workflow was green but both arms exhausted its original 64-check budget. A post-failure diagnostic with 128 checks reported residual 71 and no-residual 113. Those numbers are exploratory, not an independently preregistered result.

This successor freezes 128 total semantic checks per arm. The generator, structural enumeration, primitive language, parameter solver, controller, tasks, and held-out inputs are unchanged. The generator blob must remain cc446af6339b9f6adf1c828fb940b0bd3cf965a7. The original controller blob must remain 5d28426f2ce50032a5b291d20c93bc7acd0ff4b5. The original v1 protocol remains immutable. No additional expression constructors or target-specific dispatch are permitted.

The primary outcome is completion of both stages within 128 checks, with the exact generated program trees retained. Both arms receive the same budget and candidate enumeration. All candidate solves, independent replays, and controller verifier calls remain charged. Report actual check counts, trace, CPU time, and whether each arm succeeds. Structural pruning and algebraic search cost are not equalized, so do not call the comparison equal compute. If both arms succeed, report a cost difference, not a success-rate advantage.

The generated program must be callable on the original and the existing held-out rational families. Independent replay must establish exact polynomial identity and side conditions. The generic tree-interpreter soundness theorem must pass Lean before synthesis. A subsequent proof-interface replay may compile the selected certificate data into Lean and use the actual pinned upstream source definitions; it must not import the withheld source lower estimate. The source comparison is not a repair of an essential fluid proof.

This is an explicitly informed replication of the diagnostic, not a fresh held-out discovery. A passing result supports bounded construction and retention of reusable certificate programs from supplied primitives. It does not establish invention of the primitive language, formal correctness of the Python synthesizer, unrestricted self-development, or a new Navier–Stokes theorem. Any mathematical or search-policy change after this freeze requires a new experiment version.
