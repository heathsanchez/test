# Procedure composition

The finite experiment searches syntax trees built from constant, monomial, affine-square, interval-affine, sum, and product primitives. It does not import the old finished procedure builders. A candidate tree is a program schema; exact rational parameter matching makes it callable on new polynomials. The independent checker reconstructs every certificate identity and side condition. The unchanged cross-domain selector proposes structural candidates from the verified residual; it does not itself synthesize code.

`LIMITS.json` is the frozen search contract. Every recursive candidate solve, independent replay, original-controller verifier call, and removal check is charged. The two arms share the same total semantic-check limit. CPU time is reported separately. The previous procedure-repair source is unchanged.

The v1 64-check result is a legitimate negative outcome, not a green scientific qualification. A separate v2 may change the resource limit while retaining the same mathematical grammar. A generic Lean interpreter soundness theorem does not establish that the Python generator is formally verified. No essential fluid blow-up lemma is repaired.
