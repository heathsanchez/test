The unchanged controller is `audits/cross_domain/experiment.py` at blob 5d28426f2ce50032a5b291d20c93bc7acd0ff4b5. For procedure selection, the adapter presents a single obligation in unresolved and resolved capability states, with the same coarse observation. A candidate distinguishes the pair exactly when its certificate independently replays for the obligation. This is a capability-selection adapter, not a claim that the synthetic pair is a physical-state counterexample. Both arms have the same candidates and promotion budget. The residual arm may evaluate candidate certificates before choosing; the pass/fail-only arm chooses by order. Certificate attempts and controller verifier calls are reported separately. The experiment must not claim equal computational cost or an advantage beyond this frozen adapter.