# V8 SAIR sealed replay — adversarial interpretation

## Verdict

**PARTIAL / do not count as a clean prediction win.**

The workflow-level gate passed because the broad promotion vocabulary contained `compounding`, but the frozen semantic prediction was more specific: it expected movement from verified memory/routing into residual-conditioned constructor/schema discovery.

The first three revealed substantive commits were:

1. `Add polarized quotient continuation IR`
2. `Add multi-episode ETP compounding engine`
3. `Add TRUE-side proof inventory engine`

This supports the broader zoom decision that the programme was moving beyond Lawbook hardening into a richer compounding architecture, but it does **not** support the narrower predicted family of residual-conditioned constructor discovery.

The missed coordinate is important: the actual next representation change was a more general **continuation / quotient IR spanning polarized proof-search evidence**, followed by multi-episode ETP compounding and TRUE-side proof inventory. The controller predicted construction too early.

## Residual exposed

The current JOIN/SPECIFY step can identify the right *level* of change while still choosing the wrong *kind* of representation change.

A stronger controller needs to preserve multiple rival developmental version-space hypotheses instead of collapsing immediately to one semantic story. At this checkpoint plausible rivals should have included at least:

- residual-conditioned construction;
- a new continuation/evidence IR that unifies polarized TRUE/FALSE structure;
- broader multi-episode compounding without new constructors;
- continued Lawbook/routing hardening.

The deciding experiment should then discriminate those hypotheses before semantic invention is promoted.

## Next protocol

Require JOIN to output a **ranked version space of 3-5 rival regime changes**, not one latent explanation. Freeze the ranking and the smallest separator for each rival. Score whether the historical next intervention lies inside the top-k version space and whether the chosen separator would have distinguished the eventual winner.
