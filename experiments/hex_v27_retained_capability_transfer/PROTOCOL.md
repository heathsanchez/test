# V27 Prospective Retained-Capability Transfer

## Purpose

Test the new document claim that successful development can be compiled into retained execution capability rather than rediscovered on every encounter.

V16-V19 discovered and selected the identity-anchor + occurrence-node / ALL_ACTIVE representation law. V20-V25 proved the law and connected it to Hex's verified decision procedure.

V27 freezes a new challenge pack before any Hex outcome and applies the retained law directly, with no representation search or developmental transition.

## Frozen challenge pack

Carrier: three elements.

Every case contains two unary relations that distinguish source vertices 0 and 1, plus previously unseen higher-arity combinations. The positive target is produced by the fixed carrier permutation 0→1, 1→2, 2→0. The negative target preserves the unary vertex markers but perturbs one higher-arity tuple, so the independent source verifier must reject all carrier permutations.

Cases:

1. arities [1,1,6]
2. arities [1,1,2,3,6]
3. arities [1,1,4,6]

Each case contains one positive and one negative obligation.

## Frozen representation

No candidate representation family is searched.

For every source value, construct one role copy for every relation-position and tie all role copies to a common identity anchor.

For every true relation occurrence, construct one occurrence node and connect it to every active argument-position role.

Colors distinguish:
- each relation-position role;
- identity anchors;
- each relation symbol's occurrence nodes.

This is exactly the retained law selected before this challenge pack.

## Independent source authority

Before generating Lean:
- enumerate all six carrier permutations;
- confirm the positive pair is source-isomorphic;
- confirm the negative pair is source-nonisomorphic;
- confirm every semantic color is populated.

## External verification

Compile the retained representation to pinned HexGraphIso and kernel-check all six obligations.

## Pass

A pass requires:
- 3/3 source-positive checks;
- 3/3 source-negative checks;
- zero representation search candidates;
- zero DEVELOPMENT transitions;
- all six held-out Hex goals close.

## Claim boundary

This tests prospective reuse of the retained representation/Hex capability on a frozen new relational challenge pack. It does not establish autonomous subject selection or unrestricted action-language invention.
