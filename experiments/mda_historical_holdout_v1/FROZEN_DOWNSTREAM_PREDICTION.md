# Downstream prediction — frozen before opening Verified Decision Certificate V1 results

Source residue: the blind historical holdout adjudicator retained

- `VERIFIED_COMPARATIVE_REPAIR_CAUSALLY_USEFUL`
- `COMPARATIVE_FUTURE_STRUCTURE_STRONG_THRESHOLD`

from the earlier Verified Comparative Quotient V1 protected run.

The later experiment `verified_decision_certificate_v1` was not used to form that residue. Its precommit asks whether verifier-guided repair of a smaller decision-sufficient certificate beats one-shot certificate synthesis.

## Prediction

Without inspecting the later protected result, transfer only the **repair principle**, not the exact comparative representation:

[
\text{VERIFIED_CERTIFICATE} >
\text{ONE_SHOT_CERTIFICATE}
]

on both precommitted primary comparative endpoints:

1. downstream target accuracy;
2. decision-complete certificate rate.

Do **not** preclaim the strong thresholds (accuracy >= 0.90, decision-complete >= 0.80); those are left to the protected result.

## Falsifier

If either endpoint fails to improve over one-shot, the broader transfer of verifier-guided repair is not warranted. Retain the earlier result only at its original comparative-quotient scope.

This prediction is frozen before opening the Verified Decision Certificate V1 workflow logs.
