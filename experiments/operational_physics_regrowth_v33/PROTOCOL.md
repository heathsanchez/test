# Operational Physics Regrowth V33 — Frozen Protocol

## Question
When positivity, parallel multiplication/tensor structure, and sequential operator structure are removed from the supplied scaffold, do exact operational obligations force them back as the smallest surviving structures inside declared finite grammars?

## Arena P — composition-forced positivity
Input language: normalized symmetric rational event-pairing matrices. Local evidence checks only subset-event nonnegativity. A composition attacker may search a signed integer direction `v`, construct the rank-one positive companion `vv^T/(sum v)^2`, and test the diagonal joint event. Candidate invariant grammar, in increasing cost:
1. local subset nonnegativity;
2. local subset nonnegativity + all proper principal minors nonnegative;
3. local subset nonnegativity + all principal minors nonnegative.

Pass requires a locally admissible candidate that defeats 1 and 2 through a composition residual, while 3 excludes it and preserves positive controls.

## Arena T — parallel product regrowth
Input language: biaffine exact scalar combiners `f(a,b)=c0+c1*a+c2*b+c3*a*b`, coefficients in `{-1,0,1}`. Protected operational laws are separate-mixture affinity (built into grammar), null events `f(0,b)=f(a,0)=0`, and unit events `f(1,b)=b`, `f(a,1)=a` on a frozen exact probe set.

Pass requires exactly one survivor and exact replay that its basis-pair matrix composition is the Kronecker product. Removing null laws must restore ambiguity.

## Arena S — sequential operator regrowth
Input family ladder: 1D scalar operators, 2D diagonal operators, full 2D rational linear operators. Protected evidence is a frozen set of future signatures generated by two anonymous actions, including both `AB` and `BA`.

Pass requires order sensitivity, rejection of the commuting scalar and diagonal families, and exact replay by the full 2D family. No quantum/Jordan/C*-specific claim is permitted.

## Claim boundary
A V33 pass establishes only bounded scaffold regrowth relative to these grammars and obligations. It does not derive Born's rule, complex Hilbert space, tensor products outside the stated biaffine/basis-pair setting, unique quantum algebra, spacetime, constants, or a fundamental ontology.
