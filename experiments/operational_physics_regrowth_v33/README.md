# Operational Physics Regrowth V33

Run:

```bash
python -m unittest discover -s experiments/operational_physics_regrowth_v33 -v
python experiments/operational_physics_regrowth_v33/run.py --out /tmp/v33-evidence.json
```

V33 is a finite exact scaffold-deletion experiment with three independent arenas: positivity from composition residuals, multiplicative parallel composition, and order-sensitive sequential operators. See `PROTOCOL.md` for the frozen claim boundary.
