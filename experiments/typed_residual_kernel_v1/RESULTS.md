# Typed Residual Kernel V1 Results

## Terminal verdict

**PROVED — generic proof-carrying typed residual and transition kernel.**

Lean 4.30.0-rc2 accepted the complete standalone development without `sorry`.

## Exact earned claims

For any supplied domain interface:

1. `UnknownExpressivity` is constructible only with both a completeness proof and a certified negative showing that no continuation in the current language resolves the residual.
2. Incomplete search forbids expressive-inadequacy emission.
3. An existing resolving continuation forbids expressive-inadequacy emission.
4. Results are indexed by the exact state, language, authority, selection policy, and residual.
5. Authorization carries the declared policy obligation and its declared uniqueness obligation.
6. The transition type has separate constructors for action, identity refinement, seeking choice evidence, improving search, and proposing a generator.
7. No constructor permits `UnknownSearch` to extend the language or `UnknownChoice` to select arbitrarily.
8. Every transition carries a protected-consequence preservation proof.
9. Preservation composes over arbitrary finite proof-carrying developmental paths.

## Classification

- **PROVED:** the generic type and theorem package compiled in Lean.
- **NOT empirical:** no task-performance or acquisition-cost claim is made.
- **NOT certificate truth:** adapters remain responsible for externally meaningful predicates and certificates.
- **NOT generator genesis:** the kernel licenses where generator proposal may occur; it does not generate one.
- **NOT unrestricted self-development:** the formal result concerns typed permissions and preservation.

## Implementation repair record

Two CI failures were implementation failures involving implicit parameter inference and universe levels. They were repaired without changing the scientific obligations. The third run passed every theorem and evidence-freeze step.

## Strongest concise result

The developmental constitution is enforceable as a type system: evidence is state-indexed; each evidence form exposes only its licensed transition; expressive development requires completeness plus a certified negative; and protected consequence is preserved along every well-typed developmental history.
