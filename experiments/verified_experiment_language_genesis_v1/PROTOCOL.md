# Verified Experiment-Language Genesis V1

This is a frozen finite qualification. Q_t denotes all six constant or unary
Boolean observations on two latent factors. Completeness and absence of a
separator are enumerated separately against a rich authority vector.

Only their joint certificate inhabits UnknownExpressivityExperiment.
The lower meta-language supplies constants, projections, and generic NAND—not
a named interaction/XOR constructor. Breadth-first circuit synthesis must
construct a new denotation. Growth is rejected for incomplete searches,
old-language separators, stale language identities, and renamed old behavior.

The scientific run is immutable: positive or negative, it is not tuned.
All old-language enumeration, synthesis, novelty checks, external evidence,
policy resynthesis, and prospective computation are charged.
