# Verified Experiment-Meta-Substrate Genesis V1 — Frozen Protocol

## Question

Can certified inadequacy of the complete stateless experiment-generating
substrate lawfully cause construction of a stateful experiment-forming
capability from a lower generic finite-machine substrate?

## Frozen boundary

The old substrate is the extensional closure of `0, 1, x, y, NAND`: all 16
stateless Boolean maps of the present pair `(x,y)`. The residual histories have
identical present inputs but different previous `x`. Completeness and absence of
a separator are separate required certificates.

The lower substrate supplies only two-state finite transition-table binding,
Moore output-table binding, composition, and exhaustive finite verification.
It supplies no named history, delay, memory, previous-input, or target observer.

## Precommitted gates

1. Exhaust all 16 stateless denotations and certify none separates the histories.
2. Block extension without completeness, or whenever an old separator exists.
3. Construct a stateful observer extensionally absent from the old closure.
4. Verify the observer separates the same-present histories and changes repair authority.
5. Pass stale-certificate, rename, inert-state, state-ablation, extension-ablation,
   and separator-ablation controls.
6. Charge old closure, construction, verification, reselection, and downstream use.
7. Require the fully charged route to beat passive prospective cost.

No tuning is permitted after the first valid scientific run.
