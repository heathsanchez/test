# Semantic Certificate Causality V1 — Frozen Protocol

All five conditions receive the identical complete set of 720 adapter-instantiated macros at identical token cost and state volume. Four hidden target encodings are counterbalanced. The sole intervention is whether authority identifies one adapter as behaviorally certified.

Without a certificate, no asymmetric preference is warranted, so the controller exhaustively evaluates the complete macro set. A correct certificate licenses testing its macro first. A wrong certificate is tested, rejected, revoked, then followed by the same exhaustive fallback. Ablation and sham provide no informative certificate. Each full commuting-profile evaluation costs six verifier calls.

Pass requires correctness throughout; correct-certificate cost below no-certificate for every encoding; ablation and sham restore baseline; wrong certification cannot help; and all conditions expose exactly the same 720 macros.

A pass isolates semantic-certificate information from macro availability and generic procedural compression in this finite isomorphic qualification. It does not establish natural-domain or non-isomorphic transfer.
