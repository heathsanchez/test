# Warranted Consequential Difference V1 — Result

**Frozen candidate commit:** `978af4e94815128b4c517562f9a0bae26eea1e55`  
**Test commit:** `0c9172f392d4841ae8faf09d5e4a5a7f4da501ce`  
**Workflow commit:** `a5f8f1cabd3b7211e25e2ca0fb12410a47cbdbf8`  
**Workflow run:** 34816226419  
**Artifact:** 10336481413  
**Artifact digest:** `sha256:88dabb726a7f6c054b0399fabbe2fda263443b4a8f0bbfc9090878f31f794171`

## Verdict

```text
SURVIVED_DEDICATED_FALSIFICATION_SUITE_WARRANTED_CONSEQUENTIAL_DIFFERENCE_V1
31/31 checks passed
```

The workflow first proved the frozen candidate file was unchanged since its freeze commit, replayed prior recursive-residue, Genesis/Solvent convergence, and historical-holdout evidence, then ran the dedicated adversarial suite.

## Exhaustive results

### Epistemic trichotomy

All 65,535 nonempty compatible-world sets in the finite two-present/two-future/binary-consequence universe were classified:

- `DIST`: 4,095
- `EQ`: 15
- `UNKNOWN`: 61,425

The suite found explicit cases where:

- `forall world exists separator` holds,
- but `exists one separator forall world` fails.

Therefore a robust common separator is strictly stronger than warranted distinguishability.

It also found explicit cases where no robust separator exists but merging is not warranted, validating `UNKNOWN`.

### Joint configuration necessity

All 256 Boolean consequence functions over the 8 configurations of three structural components were enumerated.

A non-additive witness was found where:

- no component is individually indispensable in the full present,
- yet the empty atomic-indispensability residue loses protected consequence,
- while the true jointly sufficient frontier contains three incomparable two-component minima.

An explicit OR-style witness also produced the frontier:

```text
{a}, {b}
```

showing necessity does not imply uniqueness.

### Dynamic quotient

A descriptive equivalence was constructed that fails after an authorized extension, demonstrating that descriptive future-equivalence is insufficient for executable state without right congruence.

### Ground vs preference

The suite verified that:

- an unlawful cheaper candidate cannot beat a lawful candidate by economy;
- semantically equivalent lawful candidates can still be developmentally ordered by cost.

Thus ground, semantic equivalence, and developmental preference must remain distinct.

### Self-application

Continuation-generating mechanisms themselves were successfully treated as candidate presents under the same consequential relation, without adding a separate meta-law.

## Simplifications explicitly falsified

The suite defeated these alternatives:

1. define distinguishability as `exists one separator / forall worlds`;
2. if no robust separator is found, merge;
3. retain only individually indispensable components;
4. use descriptive equivalence as executable state without right congruence;
5. let cheaper structure override semantic ground;
6. treat semantic equivalence as a complete developmental preference.

## Claim boundary

This result establishes finite/exhaustive internal support for the frozen candidate and shows that several simpler formulations are incorrect.

It does **not** establish that WCD V1 is a universal law across arbitrary domains.

The next evidentiary step is heterogeneous prospective testing in which the frozen candidate is applied unchanged to domains whose developmental transitions were not used to construct the formalism.
