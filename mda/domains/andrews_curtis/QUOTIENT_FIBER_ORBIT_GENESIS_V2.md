# ACC MDA Quotient-Fiber Orbit Genesis V2

Status: frozen pre-result protocol.

Parent evidence:
- Future-consequence kernel run 34819742925: bounded false merge on ac-04402.
- Quotient-fiber Genesis run 34820909311: discovery refinements inv1 / rot1 failed prospective transfer because heldout witnesses separated on the opposite indexed relator.

Scientific question:
Does the smallest symmetry closure forced by that failure yield a transferable present-time distinction?

Development evidence is fixed to the three already-observed false-merge witnesses:
- ac-04402/current
- ac-02012/mask3
- ac-09722/mask3

The original five quotient-fiber coordinates are not enlarged with new ACC semantics. Instead we close the relator-indexed coordinates under the relator-swap action. For each exact state, form the two per-relator records:
- relator 0: (slot0, inv0, rot0)
- relator 1: (1-slot0, inv1, rot1)

For every nonempty subset of fields {slot, inv, rot}, project both records to those fields and sort the two projected records. This is the complete finite S2-orbit language generated by the existing quotient-fiber coordinates. There are exactly 7 candidate schemas.

Candidate selection:
1. Search all 7 schemas over development evidence only.
2. A schema is sufficient iff it produces no cell mixing distinct horizon-2 costs on any development witness.
3. Retain all minimum-field sufficient schemas. Do not break ties.
4. Freeze those schemas before any fresh holdout horizon-2 result is produced.

Fresh prospective holdouts, selected before reading their quotient-fiber future labels:
- ac-08534/current from acc-mda-probe-0-current
- ac-05232/mask3 from acc-mda-probe-1-mask3
- ac-02616/current from acc-mda-probe-4-current

The holdout jobs depend on the candidate-freeze job, so prospective outcomes occur only after the candidate set is frozen.

Success:
- at least two fresh holdouts must themselves contain a certified bounded quotient-fiber false merge under the unchanged selection rule;
- a frozen candidate survives iff it has zero false-merge violations on every evaluable fresh holdout;
- preserve all surviving minimum candidates.

If fewer than two holdouts are evaluable: UNKNOWN_SEARCH.
If no frozen candidate transfers: ORBIT_CLOSURE_FAILED_TRANSFER.
If one or more transfer: VERIFIED_PROSPECTIVE_ORBIT_REFINEMENT.

No solver behavior is changed. No candidate is integrated by this experiment.
