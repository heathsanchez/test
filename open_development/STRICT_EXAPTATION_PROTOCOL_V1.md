# Strict completion-delta qualification

Parent checkpoint: `17057ab3667bbc2296a8307cd9e125f863e8deb4`.

This is not a new scientific task or controller. The parent commit already froze and successfully ran `open_development/examples/exaptation_workload.json`. This follow-up reuses that exact workload and the same `Developer.run` / `EvidenceStore` transition, and hardens only the completion-delta evidence contract.

The strict gate requires: a full `ResidualEnvelope/v1` for cold B; the derived necessary constraint; a same-serialized-size sham `K_B` that is rejected; a no-requalification fixed-policy baseline with the same B acquisition budget; explicit `REUSE / EXAPTATION / EXPANSION` classification; restart and zero-acquisition held-out execution through `K_C -> K_B -> K_A`; checked source-distinct behavioral equivalence; surgical source-body, unrelated-removal, wrong-direction, raw-history, revocation and restoration controls; and the six-component developmental-cost identity.

The source capability remains frozen in its original scope, contract, verifier identity and executable body. Requalification creates a new exact dependency rather than silently broadening that source scope.

Claim boundary: the workload is repository-preregistered in the parent commit, not independently administered; the proof and role languages are finite and supplied; behavioral identity is tested only on the frozen protected observations; Python execution is not itself Lean-verified; and no metaphysical claim about discovery versus creation is made.
